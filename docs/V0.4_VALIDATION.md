# NHL Coaching Insights v0.4 — package validation

The final v0.4 source and media package was checked after the approved-palette widget-edit correction.

## Automated source checks

Run:

```bash
node scripts/validate-v0.4.mjs
```

Final result:

- 104 TypeScript/TSX files parsed with zero syntax diagnostics.
- Zero unresolved relative imports.
- 13 CSS files passed structural validation.
- Zero design placeholder strings or legacy `/__mockup/` paths.
- 49 supplied Figma icons are present; 115 static icon uses resolve to valid names.
- Dashboard customization, Calendar reuse, portrait mapping, one-row player rails, Stats selection styling, light-theme surfaces, and timestamp controls are present.
- Widget remove controls use only approved palette blues: `#4186FF` in dark mode and `#2155FC` in light mode, with `#0925AB` used for interaction states. Apple-red values are forbidden by the validator.

The machine-readable result is stored in `V0.4_SOURCE_VALIDATION.json`.

## Semantic module check

The complete hockey-dashboard module tree was checked with the system TypeScript compiler using local React/lucide declarations because package installation is unavailable in this execution environment. The check completed with zero diagnostics. See `V0.4_SEMANTIC_TYPECHECK.json`.

## Media checks

- 113 bundled images decoded successfully.
- All 17 bundled MP4 clips passed `ffprobe` validation and report positive duration, dimensions, codec, and frame-rate metadata.
- Results are stored in `V0.4_MEDIA_VALIDATION.json`.

## Data checks

- The Game 4 PostgreSQL seed regenerated byte-for-byte identically.
- The deterministic output contains 57 timestamped events and 151 player-shift intervals.
- Results are stored in `V0.4_SEED_VALIDATION.json`.

## Build note

A dependency-based Vite production build cannot be executed in this offline environment because the project intentionally does not include `node_modules`. The included GitHub Actions workflow installs the frozen pnpm dependencies and runs the production build during deployment.

## Archive checks

The final ZIP is tested with `unzip -t`. Its SHA-256 checksum is reported alongside the download link and stored in the external `.sha256` sidecar; the in-project checklist is `docs/V0.4_ARCHIVE_CHECKLIST.json`.

## Final v0.4 results

- 104 TypeScript/TSX source files passed syntax validation.
- 13 dashboard CSS files passed structural validation.
- 49 supplied icons are declared and all 115 static icon placements resolve.
- 113 bundled images decode successfully.
- 17 bundled MP4 clips contain valid video streams and positive durations.
- The Game 4 seed regenerates deterministically with 57 events and 151 official shift intervals.
- Widget remove controls use only the approved palette: `#4186FF` in dark mode, `#2155FC` in light mode, and `#0925AB` for interactive states.
- ZIP integrity and SHA-256 are generated after final packaging.
